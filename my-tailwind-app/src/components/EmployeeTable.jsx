function EmployeeTable({ employees, query, admin, onSearch, onEdit, onDelete }) {
  return <section id="directory" className="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-4 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-black">All employees <span className="ml-1 text-base font-medium text-slate-400">({employees.length})</span></h2><p className="text-sm text-slate-500">{admin ? 'Use actions to manage the directory.' : 'You have view-only access.'}</p></div><input value={query} onChange={(event) => onSearch(event.target.value)} placeholder="Search employees..." className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-indigo-500" /></div><div className="overflow-x-auto"><table className="w-full min-w-[730px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-6 py-4">Employee</th><th className="px-6 py-4">Department</th><th className="px-6 py-4">Job title</th><th className="px-6 py-4">Status</th>{admin && <th className="px-6 py-4 text-right">Actions</th>}</tr></thead><tbody>{employees.map((employee) => <tr key={employee.id} className="border-t border-slate-100"><td className="px-6 py-4"><p className="font-bold">{employee.name}</p><p className="mt-0.5 text-slate-500">{employee.email}</p></td><td className="px-6 py-4 text-slate-600">{employee.department}</td><td className="px-6 py-4 text-slate-600">{employee.role}</td><td className="px-6 py-4"><Status status={employee.status} /></td>{admin && <td className="px-6 py-4 text-right"><button onClick={() => onEdit(employee)} className="mr-3 font-bold text-indigo-600">Edit</button><button onClick={() => onDelete(employee)} className="font-bold text-rose-600">Delete</button></td>}</tr>)}{!employees.length && <tr><td colSpan={admin ? 5 : 4} className="px-6 py-12 text-center text-slate-500">No employees match your search.</td></tr>}</tbody></table></div></section>
}
function Status({ status }) { const colors = status === 'Active' ? 'bg-emerald-100 text-emerald-700' : status === 'On leave' ? 'bg-amber-100 text-amber-700' : 'bg-slate-200 text-slate-600'; return <span className={`rounded-full px-2.5 py-1 text-xs font-bold ${colors}`}>{status}</span> }
export default EmployeeTable
